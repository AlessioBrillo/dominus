# DOMINUS — Kubernetes Ingress (optional)
#
# Requires an Ingress controller (nginx-ingress, traefik, etc.).
# Uncomment and customise for your domain and TLS setup.
#
#   kubectl apply -f deploy/ingress.yaml

# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dominus
#   annotations:
#     nginx.ingress.kubernetes.io/ssl-redirect: "true"
#     nginx.ingress.kubernetes.io/proxy-body-size: "1m"
# spec:
#   ingressClassName: nginx
#   tls:
#     - hosts:
#         - dominus.example.com
#       secretName: dominus-tls
#   rules:
#     - host: dominus.example.com
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: dominus
#                 port:
#                   name: http
